# Runtime validation

Result: PENDING

Record the deployed kernel/modules pair and results for WireGuard, IPv6 policy routing, Docker
overlay storage, bridge connectivity, published-port NAT, and CONNMARK save/restore.

