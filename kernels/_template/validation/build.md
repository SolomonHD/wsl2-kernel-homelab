# Build validation

Result: PENDING

Record the exact tag and commit, source cleanliness, `olddefconfig` result, config validation,
compiler, kernel release, module-tree release, artifact names, and SHA-256 checksums.

