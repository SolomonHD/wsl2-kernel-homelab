# `<upstream-tag>` catalog record

Copy this directory to `kernels/<upstream-tag>/`, replace every placeholder, and run
`python3 scripts/catalog.py validate`. A record starts at `planned`; do not claim support or
provide a download until build and runtime validation pass and publication is reconciled.

